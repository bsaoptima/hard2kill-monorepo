End-of-session recap. Do all of the following:

1. **Review what was done this session.** Look through the conversation history and identify every meaningful action taken — files created, files edited, decisions made, strategies discussed, code changes, research done.

2. **Append to CHANGELOG.md.** Add a new dated entry at the top (below the `# Changelog` header, above previous entries) with:
   - Today's date as `## [Date]`
   - A session title as `### Session N — [Short Title]` (increment the session number from the last entry)
   - A `**Focus:**` line summarizing the session theme in one sentence
   - Bullet points for each concrete thing accomplished. Be specific — name files, features, decisions. Not generic fluff.

3. **Print a short recap to the user.** Summarize what was accomplished, what's still open, and what the logical next step would be for the next session. Keep it tight — 5-10 lines max.

4. **Update memory if needed.** If any new project decisions, user preferences, or feedback came up during the session that would be useful in future conversations, save them to memory.
